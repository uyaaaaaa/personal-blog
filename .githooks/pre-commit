#!/bin/sh
set -e
[ -d node_modules ] || {
	echo "node_modules が無く lint が走らない。node .claude/hooks/session-start.mjs を打つ" >&2
	exit 1
}
npm run lint:article
status=0
node scripts/touches-code.mjs || status=$?
if [ "$status" -ne 2 ]; then
	npm run lint:code
fi
