二重報告を止める除き方が粗く、検査が黙って抜けた。
- 判定ごと・値ごと除いたので、寄せた先が届かない経路（行内ハンドラ・除外のある composable）で誰も読まなくなった
- 同じ文字列に綴りが1つ当たると、残りの規則の判定も落ちた
- 除くなら、読む側を綴りの単位で分ける（規則の並びを持つ文字列は綴りを読む側が見ない）。判定や値の単位で外さない
- 報告済みを控えるときは、実際に報告した分だけにする。読めない綴りまで外れる
