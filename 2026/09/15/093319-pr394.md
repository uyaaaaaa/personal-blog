指摘: 記事ページの payload に、公開記事の一覧が2本入る。SearchDialog の `search-articles` と新設の `read-next-articles` が、絞り込みも並びも同じで `category` の有無だけ違う取得を別のキーで走らせていた。

直し方: `usePublishedArticles` として composable に出し、両方がこの1本を引くようにした。payload は 15433 → 14209 バイト。

観点: route に依らない取得を足すときは、同じ取得が既にあるかを先に見る（ADR 05 が対価として挙げている形そのもの）。
