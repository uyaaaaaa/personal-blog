組み立てたスタイルシートの読み手を増やしたら、既にある読み手と二重に報告した。
- 同じ文字列を1つのルールの中で2回読んだ（宣言の並びとスタイルシート）。読んだ値を控えて読み直さない
- 同じ綴りを no-restricted-syntax も Literal から読んでいた。判定ごとにどちらが読むかを表にして一方へ寄せた
- 寄せる先の綴りが別ファイルにあったので、規約の側へ移して import で引くようにした
- 「本体を持つ規則の並び」で入口を絞ったら、本体を持たない at-rule（@import）だけ素通りした。規則か at-rule が読めるかで見る
