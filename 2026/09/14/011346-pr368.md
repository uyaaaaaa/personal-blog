- must: 素の ESLint visitor は vue-eslint-parser の template を歩かず、行内ハンドラ（`@click="el.style.color = '#ff0000'"`）だけが抜け道になった。`defineTemplateBodyVisitor` で template 側にも同じ visitor を渡して直した。script に判定を足すとき、`.vue` は script と template の2経路あることを数え落とした
- suggestion: 添字のプロパティ名（`el.style['color']`）は読むのに、`el['style']` のように書き先そのものが添字だと読まなかった。綴りの読み取りを1つの関数にまとめ、書き先とメソッド名を同じもので見るようにした
- suggestion: 末尾が `style` なら要素でなくても宣言として読み、`el.dataset.style = 'color: tomato'` を誤検出した。判定から `dataset` の下を外した。経路を広げるとき、同じ綴りで CSS でないものを数えなかった
