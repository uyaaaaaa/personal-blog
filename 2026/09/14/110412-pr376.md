- 目印を `focus()` の前に付けた。隠れている要素には focus() が効かず、乗らなかった要素に付けた印は blur も keydown も来ないまま残る。状態を表す印は、狙った状態になったことを確かめてから付ける
- 実装の作りを変えて lifecycle が無くなったのに、`useX` の名前と形を残した。置き場と名前の条件（structure.md）は、作りを変えた時点で満たし直す
- 「輪郭が出ない」の判定を activeShown だけで支えた。目印はトリガにも付くので、行き先が入力欄に届いていなくても通る。無いことを見る判定は、足場ではなく行き先そのものを固定する
