- suggestion: 長さの正規表現に左端の境界が無く、`2em` で終わる識別子（`@keyframes` の名前・`animation-name`・カスタムプロパティ）の尻尾を長さとして落としていた
  - 直し方: 同じファイルの `TIME` と同じ `(?<![\w.-])` を先頭に足した
  - 元: 既存の正規表現を単位だけ足して使い回し、語彙が小さい単位を足すと前から隠れていた取りこぼしが表に出ることを数えていなかった
