- must: 長さの検査を `em` に広げた先がメディアクエリにも当たり、`no-custom-breakpoint` が通すブレークポイントの幅が書けなくなっていた
  - 直し方: 条件の `em` は初期フォントサイズが基準で宣言の `em` と別物なので、at-rule を歩くときだけ `rem` の語彙で引くようにした
  - 元: 語彙を広げるとき、その語彙を引く経路（宣言と条件）で単位の意味が同じかを見ていなかった
- 確認表の書き方: `check-css.mjs` を「終了コード 1」と書いたが、リポジトリに `.css` が無く、実際は別ディレクトリに対象を作って測っていた。証跡の条件を書かずに結果だけ書いていた
