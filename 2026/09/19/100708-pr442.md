- suggestion: `em` を `rem` として引く分岐を at-rule 全体に立てたので、`@apply` に書いたトークンの値まで語彙の外として落ちていた
  - 直し方: 基準が変わるのは `@media` の条件だけなので、`no-color-literal` と同じく at-rule を名前で絞った
  - 元: 単位の意味が変わる範囲を「at-rule」と大きく取り、同じファイル内に既にある「at-rule を名前で絞る」型を見ていなかった
