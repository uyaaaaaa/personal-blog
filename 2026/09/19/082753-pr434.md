- モジュールスコープに残る追従を、テストが止めずに終えた。後ろに足したテストが常に送っている最中の値を見る
  → `afterEach` で残ったフレームを進め切る。同じ形のテストファイル2つに同じ後始末を置いた
- 実装に説明のコメントを4件書いた（定数の意味・単位・戻す向き・共有 ref の用途）
  → すべて名前で表せるものだった。`stillFrames` / `JUMP_LIMIT_MS` / `RESTING` に出してコメントを消した
