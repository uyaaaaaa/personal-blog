- must: フロントマターの閉じ `---` を行末の形（`/\r?\n?---\r?$/`）で剥がしていたため、本文 CRLF・閉じの後ろ LF の1件だけ、読めるのに MULTIPLE_DOCS で落ちた
  直し方: 綴りの一致で剥がすのをやめ、remark-mdc が閉じと見る位置（最後の `\n---`）から先を落とす。外部ライブラリの切り出し規則に合わせる判定は、同じ規則の位置で切って形を推測しない
