must: PC幅（768px以上）ではcmd+KがSearchDialogではなくHeaderSearchのインライン入力欄にフォーカスを寄せるため、そちらのfocusByGesture呼び出しにも同じオプションを渡す必要があった。overlay-probe.mjsを1280幅で実測せず、375幅（SearchDialog経路）だけで確認していたのが原因。
suggestion: overlay-probe.mjsのshortcut系検査は`shortcut`フィールドしか見ておらず、search-inlineが宣言する`shortcutFocus`側の経路にリング検査が無かった。ショートカットで開く/寄せる経路がconfigによって名前が違う場合、検査もその名前ごとに用意する必要がある。
nits: 同じ「なぜ」の説明コメントを、オプションを使う3箇所（呼び出し元2つ+検査1つ）に書いてしまった。理由はオプションを定義する場所（gestureFocus.ts）の1箇所にだけ書けばよい。
