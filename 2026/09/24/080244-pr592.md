- must: vue-router の replace は history.state を引き継ぐので、検索から移った記事の履歴に「開いている」印が残り、そこで開いた検索が戻る操作で閉じなかった → 印を開くたびに変わる値にし、今回の値と一致する履歴だけを自分のものとして扱う
- must: 今いる記事を選ぶと vue-router が重複遷移として捨て、積んだ履歴が置き換わらずに残った → 選んだ path が今の位置（query・hash 込み）と同じなら release せずに back() で消す
