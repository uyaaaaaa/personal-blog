must 1件。検索を閉じる処理が location の watch から毎回呼ばれるのに、開いていたかを見ずにフォーカスを見えているトリガへ戻していた。戻る・進むと同一パスの遷移はレイアウトが本文へ寄せないため、検索を開いていない遷移でフォーカスだけがヘッダーの検索ボタンへ飛んだ。

直し方: 閉じる処理の先頭で、開いていないときは何もせず抜ける。開いていない遷移でフォーカスが動かないことをテストで固定した。

観点: フォールバックの戻し先を足すときは、その関数が開閉と無関係な経路からも呼ばれていないかを見る。
