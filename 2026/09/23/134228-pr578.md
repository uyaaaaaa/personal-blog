- 消すスキルの references にだけあった実測の落とし穴（headless の幅、dev 起動中の build、ダーク/ライトの値の向き）が一緒に消えた → 使う側のスキルと work rule に1行ずつ移した
- 証跡の置き場を作っていたフックを外し、そこへ書く手順（dispatch）が書けなくなる → 手順側で作らせた
- 消すスキルを参照する別スキルが残る（Codex）→ 参照元ごと消す別 PR の順で解消
