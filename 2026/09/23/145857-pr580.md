- must: draft で出した PR を更新で ready にすると判断の残りの検査を素通りする → 更新の draft: false も同じ判定に通す
- must: 本文を渡さない ready 化を判定できないまま止めた → 判定できない入力は通す（checks.md）
- P2(Codex): MCP の作成ではテンプレートのコメントがエージェントに届かない → pr-guard で本文の型を検査し、ラベルの付与を文脈で伝える
- suggestion: 確認の節の決まりの移し先が無い → 採らない。#533 のテンプレートと #578 で確認の節自体を外した
