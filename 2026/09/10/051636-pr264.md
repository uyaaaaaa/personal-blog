- 落とす検査を足したのに、対象ファイルが0件で何にも当たっていなかった。起点を引数で受け、フィクスチャを指すテストで経路を固定した。検査を足すときは「壊したら赤くなるか」を変異で確かめる
- 文面だけを共通化して判定を揃え忘れた。`<style>` 側は at-rule 名を大文字小文字そのままで比較しており、`.css` 側だけ小文字化していた。同じ規約を2経路で見るときは、判定も同じ形に揃える
