- 方向で通る入力が変わる: テスト→実装は `.spec.` を見るのに、実装→テストは `.test.` しか探していなかった。探す側も同じ綴りの集合を見て、無いときは両方をエラーに並べる形に揃えた
- 走査の入口が1本だけ: 検査の一覧を `scripts.lint` の1行からだけ読んでおり、`npm run` に委譲されると0件になって黙って通る。`Object.values(scripts)` と githook 全体を走査対象にした
