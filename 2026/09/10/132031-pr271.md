- must: 判定の範囲を広げたのに、その範囲を対価と戻す条件に書いていた ADR を同じ変更で直していなかった（ADR 12「.css は見ない」「Stylelint に移す」、ADR 15「.css は見ない」）。覆した判断を ADR 17 として足し、失効した行を落として直した
- suggestion: 検査の文面が、対象のファイルには存在しない抑制手段（<script> の eslint-disable）を案内していた。例外の置き場が .vue の <style> だけであることを文面に出し、抑制手段は増やさずに直した
- nits: 参照元を1つに減らしたのに export を残していた（IMPORT_MESSAGE）。export を外して使う場所に閉じた
