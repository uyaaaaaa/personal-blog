- 綴りの区切りを `.` だけに限ったため、`@fontsource/...` と `@nuxt/fonts` が素通りした。区切りに `/` と終端を足し、nuxt の `css` も指定子の枠で見るようにした
- 検査の網を「外部かどうか」で分けたが、引いた先の CSS は内外どちらも lint が読まない。`@import` を一律で落とすようにした
- 落ちる経路を selector に分けて並べたため、両方に当たる文字列が2回報告された。1本の selector にまとめた
