- 規約・ADR に理由と「やらないこと」を書きすぎ（書き手）→ 理由と否定の記述を削り、何をするかだけに絞った
- 検査の規約にテストの本数を書くのは不要、エージェントに任せる（書き手）→ checks.md からテストの指定を外した
- lint や依存検査が守っているとして規約の行を消したが、検査は import しか見ておらず、import を伴わない違反（ui/ が題材を既定値で持つ）が素通りになる（レビュー）→ 範囲を絞った1行として戻した
- 実装と食い違うとして imports.md を消したが、誤読で、content/ の部品の扱いを書いた唯一の規約だった（レビュー）→ 誤読されない文に書き直して戻した
