accent の使い方が DESIGN_GUIDELINE の原則3と食い違ったまま（must）

issue の完了条件（件数は accent、hover は線が主色）を実装したが、原則3 は accent を「意味のある状態」に限っていた。
実装を戻さず、覆した側として原則3 を目印まで広げ、ADR 19 と索引の行を同じ変更で足した。

取りこぼした観点: issue の完了条件が既存の原則を覆すとき、実装と同じ変更で文書を直す必要がある（ui-change に書いてあるが、issue 対応では踏まなかった）。
