- 指摘: 本文の PC 段を `theme.extend.typography.lg` で持ったため、Tailwind typography の既定の段 `prose-lg` を上書きしていた。既定が持つ h1・引用・コードブロックの em のサイズ表と混ざる
- 直し方: PC 段を既定に無い名前（`wide`）で持ち、上書きは本文の font-size / line-height と inline code に絞った。既定の段の名前を上書きしたら落ちるテストを足し、寸法トークン側にも同じ形のテストを置いた
- 拾えなかった理由: 「既定の名前の意味を変えない」を寸法（`sizes`）だけの話として読み、プラグインが持つ段の名前を同じ線の内側と見ていなかった
