判定の突き合わせで、数えた件数を下限ではなく一致条件として扱った。持ち越した未対応と、自分の PR に REQUEST_CHANGES を返せない制約の両方で、正しいレビューが落ちた。緩い側に倒した判定だけを落とす形に直した。
上限の数え方で、初回のレビューを出し直しの回数に含めた。手順書の回数が何を数えているかを読み違えた。
証跡の有無を部分一致で見たため、名前に測ったものを含むだけのファイルが要件を満たした。完全一致に直した。
