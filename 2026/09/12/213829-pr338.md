- 押せないタグに accent のホバーを付けていた（原則3: accent は意味のある状態にだけ使う）。/tags/<slug> のリンクにして、ホバーを意味のある状態にした
- sticky の留まる位置（100px）と留まる高さ（100vh - 96px）を別々の値で持ち、下端が 4px 画面外に出ていた。高さを位置から引く形にして正本を1つにした
- 言語を指さない綴り（text / txt / plaintext）のフェンスに、中身の無いラベルの帯が出ていた。綴りの一覧で落とした
- PC の目次は CONTENTS、モバイルの開閉ボタンは Contents と表記が割れている指摘（nits）。issue が書き分けており、後者は節見出しではなくボタンなので採らなかった
