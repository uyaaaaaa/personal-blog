# PR #481 で受けた指摘

- **must: 節番号の参照を消し切れていない。** 同じ表の下の行に `enforce` の2・4・5・6 が残っていた。grep のパターンが数字の区切り（`・`）を数えておらず、拾えたつもりで拾えていなかった。→ 見出し名に替え、区切り付きの番号も当たるパターンで取り直した
- **suggestion: 責務を移したのに description が元のままだった。** `assign` の説明が「着手から PR の見届けまで進める」のままで、切り出した `followup` と守備範囲が重なっていた。→ 「PR を出すまで」に狭めた。責務を移したら、本文だけでなく description も同じ変更で動かす
