- 見届けのフックが数える発火が MCP の呼び出しだけで、review-guard が同じく見る `gh workflow run` を取りこぼしていた。同じ規約が成り立つ経路は、数える側と検査する側で同じ読み方に寄せる（`review-rules.mjs` の `dispatched()` に一本化し、PostToolUse の matcher に Bash を足した）
- ワークフロー名の突き合わせ（`plain`）が2ファイルに分かれていた。綴りの正本は1つにして import して引く
- 落ちても数える側の catch を、テストで固定していなかった。逃げ道の分岐こそ例を置く
- エージェントを起こすだけの設計では、返った JSON を渡さずに終われた。見届けは「出したところ」まで数える
- 作業ツリーのレビューは CI が無いので、`verified` の元を手順に書いておく
