- 置き場を指す名前（inline / row / control）にしたら、同じ置き場が別の名前になって名前が嘘になった。段の名前（small / base / large）に替えた
- 共通の殻に v-html の口（shape prop）を足したら、使うのは1箇所なのに全アイコンが口を抱えた。文字列を持つ側でスロットに包んだ
- 値の一覧（ICON_SIZE_CLASS）を export していた。外が引くのは型だけなので外した
- 実装の在り処を書いた文書の一行が、ファイルを移して古くなった。ファイル名ごと落とし、理由だけ残した
- 持ち主と選び方を説明するコメントを書いた。prop が唯一の口なら実装で読めるので消した
