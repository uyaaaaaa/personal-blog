- Icon.vue に大きさを持たせられないか（ask）→ 殻は複数の大きさに使われており、既定を置くと呼び出し側が上書きで戦う。大きさの持ち主は用途の決まる個別アイコン側だと返し、寄せる変更は別 issue に切り出した
- ページングのシェブロンの svg が同じファイルに4回（imo）→ 重複は実在。寄せ先が上と同じ層なので同じ issue にまとめ、このPRの範囲外として本文に明記した
