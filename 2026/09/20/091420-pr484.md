指す先を `.claude/settings.json` の matcher にしたが、matcher は Bash 全体に当たるのに `decide()` は `gh workflow run` の形にしか当たらず、curl と `gh api` の dispatches が素通りする、という指摘。
指す先を、実際に検査している `.claude/hooks/review-guard.mjs` に変えた。読み手が「拾われる」と読む範囲と、検査が当たる範囲を一致させる。
