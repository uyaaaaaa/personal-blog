- 別ファイルが持つ綴りの一覧を正規表現で拾っていた（must）。一覧を配列で export し、検査は import で引く形に変えた。文面はその配列から組み立てる
- 規約が成り立つ経路のうち文面だけを固定し、当たる対象（設定の files）を見ていなかった（suggestion）。対象は eslint の設定の解決に聞く形で検査に入れた
- 出した直後に main が進んでコンフリクトしていた（must）。レビューより前のマージ取り込みで解消済みだった
