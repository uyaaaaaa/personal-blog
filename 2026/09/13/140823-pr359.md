- must: `docs/adr/` の走査が `.md` 以外も拾い、`.gitignore` 済みの `.DS_Store` 1つで lint が終了コード 1。pre-commit が `npm run lint` を打つため以後どのコミットも通らない。→ `article-files.mjs` と同じく `.md` だけを拾う側に倒した
- suggestion: 索引の行を位置で突き合わせていたため、余った1行から後ろがずれ、実在する ADR が「無い」と報告される。→ ファイル名で突き合わせ、余った行だけを行番号付きで名指し、並びは別に見る
- suggestion: 連番を機械が要求する以上 ADR を消せば詰め直しが起き、索引の外（`docs/` と `.claude/` に20行）の ADR へのリンクが黙って切れる。→ 同じ走査で `.md` を辿り、`docs/adr/` を指す相対リンクの先の有無を見る
