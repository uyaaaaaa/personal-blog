- 抑制の経路を数え落とした。`eslint-disable` だけを見て、重さを書き換えるインラインの設定 `/* eslint <rule>: "off" */` を数えていなかった。これは位置に依らずファイル全体に効くので、ブロックの並びを固定しても塞げない。直し方: 検査を抑制の綴り2つに広げ、ファイルの中で重さを書き換えること自体を落とすようにした（重さの決定は eslint.config.mjs だけ）
