- must: 現在項目の印に aria-current="page" を使い、同じ文書のページ送りが出す "page" と2つ並んだ。ページ2以降は、開いていないパスを指す側が page を名乗っていた。集合の中の現在項目は "true" にして直した。印の値を決めるとき、同じ文書に "page" を出す別の部品が居ないかを先に見ていなかった
- nits（採らず）: hover:text-main が基底と同じで無意味という指摘。非 scoped な a:hover が .text-main より詳細度が高く、外すとホバーで accent になる。算出値を測って残す判断にした。基底クラスだけを見て打ち消しの要否を判断できない
