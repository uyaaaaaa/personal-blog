- コマンド文字列を判定する検査で、heredoc の本体を区間から外していなかった。シェルに渡す文字列とコマンドを区別してから切るよう直した
- `git branch` の位置引数を、作成では起点、改名では旧名と読み違えた。作成は最初、改名は最後を見るよう分けた
- `rebase` をサブコマンド名だけで数え、`git pull --rebase` と `-c pull.rebase=` を落としていた。同じ判定に載せた
- 綴りを変えた呼び出し（括弧・前置きの環境変数・絶対パス）が判定に載らなかった。先頭のトークンを正規化してから見るよう直した
