- 静的な属性値（`VLiteral`）を見落とした。式の `Literal` を見る選択子を書いたとき、テンプレートの静的な属性は別のノード型になることを数えていない。`!important` が静的と束縛を対で持っているのに、それに揃えなかった
- 設定ファイルの上書きを数えなかった。`*.config.ts` は `no-restricted-syntax` を別の配列で上書きするので、共有の配列に足しても届かない。経路を数えるとき、設定ファイルの override を見ていない
- 綴りで見る判定が `overscroll-behavior` を巻き込んでいた。issue 本文が「別物で3件ある」と先に書いていたのに、クラス側だけ境界を入れて宣言側に入れ忘れた
