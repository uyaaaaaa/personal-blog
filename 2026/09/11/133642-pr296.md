- suggestion: `node` の直後だけを見ていたため、フラグを挟んだ hook を通していた。`-` で始まる語を読み飛ばす形にし、綴りのテストに足した
- suggestion: 状態を `.verify/` に置いたが、`verify` が確認フェーズの頭で作り直すためセッションの途中で消えていた。OS の一時ディレクトリに移し、消えない置き場になった分を `session_id` で分けた
