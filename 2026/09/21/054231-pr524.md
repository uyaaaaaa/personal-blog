- 検査を足したとき、規約が成り立つ経路（`^(?:tests/)?` の2つの根）のうち片方にしかテストの木を置いていなかった → 根を配列にして同じ表を両方で回す
- 設定が入力を弾いて止まる分岐（綴り・ui の有無・領域0件）を1つもテストで踏んでいなかった → 一覧を `vi.doMock` で差し替えて読み直し、`rejects.toThrow` で理由ごと固定する
- テストが規則だけを渡し、出荷する形の `options` を渡していなかった → `...configuration.options` を広げて渡し、規則の外側（`exclude`）を緩めた回も落ちるようにする
- 別のファイルから導いていること自体を見ておらず、直値に戻しても緑で抜けた → 差し替えた一覧の綴りが規則の `path` に出るかを見る。`from` 側だけでは行き先の綴りの退行が残る
- ヘルパの引数に `it` を使い、vitest の `it` と綴りが衝突していた → 中身を指す綴り（`pair`）にする
