# CLAUDE.md

Nuxt 4 + Nuxt Content 3 の個人技術ブログ。Cloudflare Pages に静的生成でデプロイしている。

**原則としてドキュメントは書かない。実装が正で、実装を読めば分かることは書かない。** 文書は実装と一緒には更新されず必ず乖離し、乖離した文書は無いより有害。実装・lint・Git 履歴・issue が持てるものは、そちらに持たせる。

**方針を覆したときだけ、実装と同じ変更で文書を動かす。** 構造・依存方向・公開 URL に関わる判断を覆したら、`.claude/rules/docs.md` の基準に当てて必ず ADR を書く。既にある記述は同じ変更で直すか消す。
