name: Typecheck

on:
    pull_request:
    push:
        branches: [main]
    workflow_dispatch:

permissions:
    contents: read

concurrency:
    group: typecheck-${{ github.ref }}
    cancel-in-progress: true

jobs:
    changes:
        uses: ./.github/workflows/changes.yml

    typecheck:
        needs: changes
        # 記事だけだと判定できた回にだけ飛ばす。!cancelled() が無いと changes が落ちた回も飛ぶ
        if: ${{ !cancelled() && needs.changes.outputs.code != 'false' }}
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v7
            - uses: actions/setup-node@v7
              with:
                  node-version: 22
                  cache: npm
            - run: npm ci
            - run: npm run typecheck
