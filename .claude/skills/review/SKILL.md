---
name: review
description: "このリポジトリのコードレビュー。`code-review` の指摘をこのリポジトリの基準で絞り、Approve 可否を先頭に置いた短いコメントで返す。「レビューして」「#123 をレビューして」「この差分見て」という依頼で使う。"
---

# レビューの手順

指摘の探索は組み込みの `code-review` に任せる。**このスキルが決めるのは2つだけ。**

1. **何を捨てるか**（lint / build / テストが持つ領分と、確信の持てないものを落とす）
2. **どう書くか**（Approve 可否を先頭に、1コメント1論点で短く）

レビューは差分を読ませる作業ではなく、**書き手の手を止めるかどうかの判定**。判定に効かない指摘は、正しくても書かない。

## 全体

| フェーズ | やること | 終了条件 |
| :--- | :--- | :--- |
| 1. 集める | `code-review` を回す | 生の指摘が手元にある |
| 2. 捨てる | §3 の表で落とす | 残ったものが全部「消せない理由」を言える |
| 3. 判定する | Approve 可否を1つ決める | 判定と理由が1行で言える |
| 4. 書く | §5 の型で整形する | 1コメント1論点、6行以内 |
| 5. 出す | サマリ＋インラインで投稿する | 先頭に判定が載った |

---

## 1. 集める

`code-review` スキルを呼ぶ。**`--comment` は付けない。** 投稿はこのスキルが行う（フォーマットを握るため）。
`--fix` も付けない。直すのは書き手の判断。

| 対象 | 渡すもの |
| :--- | :--- |
| 作業ツリー | 引数なし（`main` からの差分） |
| PR | PR 番号 |
| ブランチ・パス | そのブランチ名 / パス |

effort の既定は **medium**。生成物・ルーティング・データ取得に触る差分だけ `high` にして、§3 で絞り直す。

**差分の範囲を先に確認する。** `git diff main...HEAD --stat` の出力を見てから読む。

## 2. 捨てる

### 機械が持っている領分は書かない

`npm run lint` / `npm run build` / `npm test` / Git フックが落とすものを人が指摘しても、検査と二重になるだけ。

**効いている検査の一覧は持たない**（→ [ADR 11](../../../docs/adr/11-no-enforcement-inventory.md)）。写した表は追従されず、必ず嘘になる。**どれが機械の担当かは、一覧を引かずに実行して確かめる。**

- レビューの前に `npm run lint` と `npm test` を通す。**落ちたものは検査の担当**なので、指摘には起こさない。伝えるならサマリの1行で足りる
- 依存が無いなどで通せないときは、サマリの「実測」に未実施と書き、機械が見ていそうな範囲（整形・import・循環・型）は指摘を控える
- 残るのは**落ちないのに壊れているもの**だけ。それが §3

### このリポジトリでは指摘そのものが間違いになるもの

| 書かない指摘 | 根拠 |
| :--- | :--- |
| 「コメントを足したほうがいい」 | 原則コメントを書かない（`.claude/rules/comment.md`） |
| 「ドキュメントに残したほうがいい」 | 原則書かない。残すのは ADR の基準に当たるときだけ（`.claude/rules/docs.md`） |
| 「共通化・抽出したほうがいい」（1箇所しか使っていない） | 抽出は2回目の使用時（`.claude/rules/structure.md`） |
| 「レイヤーを分けたほうがいい」「barrel file を作ろう」 | 型別フラット構成を維持する（[ADR 04](../../../docs/adr/04-flat-directory-by-type.md)、`.claude/rules/imports.md`） |
| 「`prefers-reduced-motion` を見るべき」 | 意図的に参照しない（`.claude/rules/style.md`） |
| 命名・言い回しの好み、記事本文（`content/`）の文章表現 | 判定に効かない。記事は `article` スキルの範囲 |

### 確信が持てないものは書かない

- 本文に「かもしれません」「可能性があります」が入るなら**落とす**。読んで判断しただけのものは指摘ではなく質問。
- どうしても伝えるなら、指摘ではなく**サマリの1行**にまとめる。
- **上限の目安は5件。** 超えたら重い順に落とす。多く挙げるほど、重い1件が埋もれる。

## 3. 見るもの

機械が持っていない領域だけが残る。上から重い順。

| 観点 | 何を見るか | 根拠 |
| :--- | :--- | :--- |
| 生成物に出るか | `onMounted` 依存の分岐、`v-if` での出し分け、静的生成で消える表示 | `verify`、ARCHITECTURE |
| データの流れ | 取得を呼ぶ層、公開制御と絞り込みの有無、props で受けるべき部品 | `.claude/rules/structure.md` |
| 状態の正本 | URL が持つべき状態の複製、同じ値の正本が2箇所 | 同上 |
| スタイル | トークンを経由しない直値、`dark:` での色分岐、独自ブレークポイント、`!important` | `.claude/rules/style.md`、DESIGN_GUIDELINE |
| コメント | 足されたコメントが `comment.md` §2 に当たるか。当たらないなら消す指摘 | `.claude/rules/comment.md` |
| 方針との整合 | 方針を覆したのに ADR / GUIDELINE が同じ変更で直っていない | `.claude/rules/docs.md` |
| 実測の主張 | PR の「確認」に、走らせていないコマンドの結果が書かれていないか | `verify` |
| コミット | 件名が「何を」と「どう変えたか」を両方言えているか。1コミットに2つの変更が入っていないか | `.claude/rules/commit.md` §2・§3 |

**方針に触れる差分は、先に該当文書を読んでから指摘する。** 読まずに「ガイドラインに反する」と書かない。

## 4. 判定する

**判定は必ず1つ選び、サマリの先頭に置く。** 迷ったら `Comment`。

| 判定 | 意味 | 使うとき |
| :--- | :--- | :--- |
| `Approve` | このまま出せる | 要対応が0件 |
| `Comment` | 直さなくても出せる | 任意の指摘だけ。判断は書き手に渡す |
| `Request changes` | 直さないと出せない | 壊れる・生成物が変わる・方針に反する、のどれか1つ以上 |

**`Request changes` は「壊れる」ときだけ。** 好みや改善提案では出さない。

## 5. 書く

### サマリコメント（1本、必ず出す）

先頭行が判定。理由は1行で言い切る。

```md
**判定: Request changes** — 目次の開閉が生成 HTML に出ず、初期表示が閉じたままになる。

- 要対応 1件 / 任意 2件
- 実測: `npm run lint` `npm run build` は未確認（CI 待ち）
```

- 先頭行は **判定 + ダッシュ + 理由1文**。`Approve` なら理由は不要で判定だけでよい。
- 続けるのは**件数**と**実測の状態**の2行まで。
- 前置き、総評、褒め言葉、差分の要約は書かない。

### インラインコメント（1件1論点）

```md
**静的生成の HTML では閉じたままになる**

`onMounted` 後にしか開閉が決まらないため、初期状態がビルド時に焼き付く。
CSS で出し分けるか、初期値を props で受ける。
```

| | 決まり |
| :--- | :--- |
| 見出し | 1行。**何が起きるか**を言い切る。「〜すべき」ではなく「〜になる」 |
| 本文 | 3行以内。1行40字目安。理由 → 代案の順 |
| 合計 | 見出し・本文・コード片を合わせて6行以内 |
| 論点 | 1コメント1つ。2つあるなら2コメントに割る |
| コード片 | 提案が1〜5行で書けるときだけ。それ以上は文章で書く |

**書かないもの。** 差分の写し、前置き（「素晴らしい変更ですが」）、「かもしれません」、他の指摘への参照、ソースコードレベルの情報を並べただけの説明。

## 6. 出す

- 投稿は GitHub MCP のレビューツール（`gh` は使えない）。pending review を作る → コメントを足す → submit の順
- サマリはレビュー本体の本文に置く。インラインは該当行に付ける
- 作業ツリーのレビュー（PR が無い）は投稿せず、同じ型で会話に返す
- 各コメントの末尾に `_Generated by [Claude Code](https://claude.ai/code)_` のフッターを付ける
- **投稿したら、判定と件数だけを報告して終わり。** 直すかどうかは書き手が決める
