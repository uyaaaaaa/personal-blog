# 落とす基準

## 落とすもの

- 検査で落ちているもの
- 対象ファイルの `.claude/rules/` に反するもの
- 断定できないもの。「かもしれません」が入っているなら落とす

## 件数の上限

**合計5件まで。うち `imo` と `nits` は合わせて2件まで。**

上限に収まらないなら、基準を緩めるのではなく弱いほうから落とす。

## 補足

記事本文（`content/`）は `article` の範囲。ただし **PR の「確認」に、走らせていないコマンドの結果が書かれていないか**は見る。
