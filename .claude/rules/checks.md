---
paths:
  - "eslint.config.mjs"
  - "eslint-rules/**"
  - "scripts/check-*.mjs"
  - ".githooks/**"
---

# 検査を足すときのルール

- **足した検査は、破る例が落ちることと通す例が通ることの両方をテストで固定する。** どちらかを欠くと、何にも当たっていない検査か、正当な入力を落とす検査が入る。
- **同じ規約を複数の経路で見るときは、判定を同じ形に揃える。** 文面だけ共通化すると、経路ごとに通る入力が変わる。
