---
paths:
  - "eslint.config.mjs"
  - "eslint-rules/**"
  - "scripts/check-*.mjs"
  - ".githooks/**"
---

# 検査を足すときのルール

- **足した検査は、破る例が落ちることと通す例が通ることの両方をテストで固定する。** どちらかを欠くと、何にも当たっていない検査か、正当な入力を落とす検査が入る。
- **規約が成り立つ経路を実装と設定から数え、全部を同じ判定で見る。** 数え落とすと片側だけを塞いだ検査になり、経路ごとに判定を分けると通る入力が変わる。
