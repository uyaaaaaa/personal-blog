---
paths:
  - "tests/**"
---

# テスト

要否は「消すと、どんな現実的な不具合を見逃すか」で決める。無いか、別のテストが捕まえるなら要らない。

- 要らない: 実装の写し、型が保証していること、モックの自己検証、隣の `it` の部分集合、判定を import して並べ直しただけのもの、内部の値や呼び出し回数への依存
- 要る: 検査の破る例1本と通す例1本、util の唯一のテスト、実行環境が持たないもの（レイアウト・フォーカス移動）を補う部分
