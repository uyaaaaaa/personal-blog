---
paths:
  - "**/*.vue"
  - "**/*.ts"
  - "**/*.mjs"
  - "**/*.cjs"
  - "**/*.yml"
  - ".githooks/**"
---

# コメントのルール

**原則としてコメントは書かない。** 補足が要るのは実装が言い切れていない合図なので、先に名前・分割・型で表す。

## 残してよいもの

リポジトリ内を全部読んでも分からず、理由がコードの外にあるものだけ。迷ったら書かない。

- **外部の仕様・ライブラリの都合。** 例: remark-mdc が生成するノードの形
- **一見不要なコードが必要な理由。** 次の人が消さないためのもの
- **意図的に書いていないこと。** 例: `display` は Tailwind の `md:` に統一しているのでここで指定しない
- **名前から意味が読み取れない値。** 例: `'-'` は初期状態が閉、`'+'` は開

`@ts-expect-error` `eslint-disable` などの機能を持つディレクティブはコードなので、この規約の対象外。

「なぜ」を1〜2行で書く。それ以上要るなら、命名や構造で表現し直す。実装を変えたらコメントも直すか消す。
