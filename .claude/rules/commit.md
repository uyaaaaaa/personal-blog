---
paths:
  - "**"
---

# コミット

件名に「何を」と「どう変えたか」を書く。

| 書く | 書かない |
| :--- | :--- |
| `View Allの判定をその幅で実際に見えている件数に合わせる` | `View Allの判定を直す` |

- ファイル名・件数・数値・issue の文面は書かない。本文は任意で「なぜ」だけ
- 整形だけのコミットは分け、ハッシュを `.git-blame-ignore-revs` に足す
- 実測用の一時的な細工は含めない
