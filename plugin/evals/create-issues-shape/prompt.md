---
max_turns: 15
allowed_tools: [Read, Glob, Grep, Skill]
---

タグ「日本語」の一覧ページ /tags/ を開くと、タグ「設計」の記事まで混ざって出る。日本語だけのタグは slug が空になって、全部同じページに落ちているみたい。コードは resources/app/utils/slug.ts。
toSlug に、空になったらハッシュを使うフォールバックを入れて直したい。
終わったら、日本語だけのタグでもそのタグの記事だけが出ていればいい。URL が変わる既存のタグは無いようにしたい。これを issue にして。

GitHub には繋がっていない。発行する代わりに、タイトル・ラベル・本文を最後にそのまま書き出して。質問があれば、それを最後に書き出して終えてよい。
