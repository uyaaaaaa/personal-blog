---
max_turns: 10
allowed_tools: [Read, Glob, Grep, Skill]
---

気になってることをまとめて issue にしてほしい。
ダークモードで、コードブロックの行番号が背景とほぼ同じ色で読めない。どのコードブロックでも起きる。読める濃さになっていればいい。
あと RSS フィードに記事の説明文が入っていない。フィードリーダーで開くと、タイトルしか出ない。各記事の frontmatter の description が入っていればいい。

リポジトリのコードは渡していない。GitHub にも繋がっていない。発行する代わりに、タイトル・ラベル・本文を最後にそのまま書き出して。質問があれば、それを最後に書き出して終えてよい。
